# SVHighlights dependencies — covers benchmark/ and tf_selector/
# (preprocessing + TF-SELECTOR model inference).
#
# Install into a single conda environment; see README.md ("Environment setup").
#
# Installed separately (see README.md):
#   * PyTorch (torch / torchvision / torchaudio) -- from the CUDA-specific PyPI
#                                                   index (also pulls in the nvidia-* wheels).
#   * ffmpeg / ffprobe  -- conda install -c conda-forge ffmpeg
#   * CLIP              -- pip install git+https://github.com/openai/CLIP.git
#                          (only for benchmark/eval_clip_similarity.py)
#   * WhisperX          -- pip install git+https://github.com/m-bain/whisperX.git
#                          (only for tf_selector/transcribe.py)
#   * TransNet V2       -- git clone https://github.com/soCzech/TransNetV2
#                          (only for tf_selector/shot_boundary.py)
#
# Tested with Python 3.9; the versions below are the ones the pipeline was run with.

# --- Preprocessing extras (benchmark/ + tf_selector/ preprocessing scripts) ---
natsort==8.4.0
pydub
scikit-image==0.24.0
scikit-learn

# --- TF-SELECTOR model inference (frozen) ---
accelerate==1.4.0
annotated-types==0.7.0
anyio==4.9.0
blessed==1.20.0
cachetools==5.3.2
certifi==2026.5.20
charset-normalizer==3.4.7
distro==1.9.0
einops==0.8.1
exceptiongroup==1.3.0
filelock==3.19.1
fsspec==2025.10.0
gpustat==1.1.1
h11==0.16.0
hf-xet==1.5.0
httpcore==1.0.9
httpx==0.27.2
huggingface_hub==0.36.2
idna==3.16
Jinja2==3.1.6
jiter==0.9.0
MarkupSafe==3.0.2
mpmath==1.3.0
networkx==3.2.1
numpy==2.0.2
nvidia-ml-py==12.535.133
nvitop==1.3.1
openai==1.79.0
packaging==26.2
pandas==2.2.3
pillow==11.3.0
psutil==7.2.2
pydantic==2.11.4
pydantic_core==2.33.2
python-dateutil==2.9.0.post0
pytz==2026.2
PyYAML==6.0.3
regex==2026.1.15
requests==2.32.5
safetensors==0.7.0
sentencepiece==0.2.0
six==1.17.0
sympy==1.14.0
termcolor==2.4.0
timm==1.0.19
tokenizers==0.21.4
tqdm==4.67.3
transformers==4.49.0
triton==3.0.0
typing-inspection==0.4.0
typing_extensions==4.15.0
tzdata==2026.2
urllib3==2.6.3
